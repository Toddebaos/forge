apiVersion: v1
kind: Service
metadata:
  name: mongodb-exporter-percona
  namespace: monitoring
  labels:
    app: mongodb-exporter
spec:
  selector:
    app: mongodb-exporter
  ports:
    - port: 9216
      targetPort: 9216
      name: metrics