name: CI — forge-ingestor

on:
  push:
    paths:
      - 'apps/forge-ingestor/**'
    branches:
      - main
  workflow_dispatch:  # möjliggör manuell triggning från GitHub UI

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout kod
        uses: actions/checkout@v4

      - name: Sätt upp Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Gör mvnw körbar
        run: chmod +x apps/forge-ingestor/mvnw

      - name: Kör tester
        working-directory: apps/forge-ingestor
        run: ./mvnw test

      - name: Logga in på GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Sätt lowercase owner
        run: echo "OWNER=$(echo ${{ github.repository_owner }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Bygg och pusha Docker-image
        uses: docker/build-push-action@v5
        with:
          context: apps/forge-ingestor
          push: true
          tags: |
            ghcr.io/${{ env.OWNER }}/forge-ingestor:latest
            ghcr.io/${{ env.OWNER }}/forge-ingestor:${{ github.sha }}

      - name: Uppdatera image-tag i deployment
        run: |
          sed -i "s|ghcr.io/toddebaos/forge-api:.*|ghcr.io/toddebaos/forge-api:${{ github.sha }}|" \
          infra/base/apps/forge-ingestor/deployment.yaml
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add infra/base/apps/forge-ingestor/deployment.yaml
          git commit -m "ci: update forge-ingestor image to ${{ github.sha }}"
          git push origin main